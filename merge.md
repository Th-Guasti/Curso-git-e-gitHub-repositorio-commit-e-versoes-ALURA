git checkout -b "nome da branch" (cria uma branch) -> git switch "nome da branch" (troca pra branch) -> git switch main (voltar para main) -> git push origin main (envia o que tinha na branch para main)